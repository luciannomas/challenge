{
  "name": "company-adhesion-lambda",
  "version": "1.0.0",
  "description": "AWS Lambda function for company adhesion",
  "main": "dist/handler.js",
  "scripts": {
    "build": "tsc",
    "package": "npm run build && zip -r function.zip dist/ node_modules/"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "mongodb": "^6.3.0",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.119",
    "@types/node": "^20.0.0",
    "@types/uuid": "^9.0.2",
    "typescript": "^5.0.0"
  }
}

